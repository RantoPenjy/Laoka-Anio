{% extends 'base.html.twig' %}

{% block title %}Lisitry ny laoka rehetra{% endblock %}

{% block body %}

<div class="container mt-3">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="">Lisitry ny laoka rehetra misy</h1>
        <a href="{{ path('add_plat') }}" class="btn btn-primary btn-sm mt-2">
            <i class="fas fa-plus"></i>
            Hanampy laoka vaovao
        </a>
    </div>
    <table class="table table-bordered" id="datatable-plat-list">
        <thead>
            <tr>
                <th>ID</th>
                <th>Anaran'ny laoka</th>
                <th>Andro</th>
                <th>Fomba fikarakarana (recette)</th>
                <th></th>
            </tr>
        </thead>
{#        {% for plats in plat %}#}
        <tbody>
{#            <tr>#}
{#                <td>{{ plats.name }}</td>#}
{#                {% if plats.day == true %}#}
{#                    <td>Andro alahady na fety</td>#}
{#                {% else %}#}
{#                    <td>Andro tsotra</td>#}
{#                {% endif %}#}
{#                {% if plats.recipe == '' %}#}
{#                    <td>Mbola tsy misy</td>#}
{#                {% endif %}#}
{#                <td class="d-flex justify-content-around">#}
{#                    <a class="btn btn-outline-secondary" href="#}{# path('edit_plat', {id:plats.id}) #}{#" data-bs-toggle="tooltip" data-bs-placement="left" title="Amboarina">#}
{#                        <i class="fa fa-edit"></i>#}
{#                    </a>#}
{#                    <a class="btn btn-danger" href="#}{# path('delete_plat', {id:plats.id}) #}{#" data-bs-toggle="tooltip" data-bs-placement="right" title="Fafana">#}
{#                        <i class="fa fa-trash"></i>#}
{#                    </a>#}
{#                </td>#}
{#            </tr>#}
        </tbody>
{#        {% endfor %}#}
    </table>
</div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        let dataPlat = {{ plat | json_encode | raw }};
    </script>
    {{ encore_entry_script_tags('custom') }}

{% endblock %}
