{% extends 'base.html.twig' %}

{% block title %}Lisitry ny laoka rehetra{% endblock %}

{% block body %}

    {% include 'header.html.twig' %}

    <div class="container mt-3">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="">Lisitry ny laoka rehetra misy</h1>
        <a href="{{ path('add_plat') }}" class="btn btn-primary btn-sm mt-2 rounded-5">
            <i class="fas fa-plus"></i>
            Hanampy laoka vaovao
        </a>
    </div>
    <table class="table table-bordered mt-4" id="datatable-plat-list">
        <thead>
            <tr>
                <th>ID</th>
                <th>Anaran'ny laoka</th>
                <th>Andro</th>
                <th>Fomba fikarakarana (recette)</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        </tbody>
    </table>
</div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        let dataPlat = {{ plat | json_encode | raw }};
    </script>
    {{ encore_entry_script_tags('custom') }}

{% endblock %}
